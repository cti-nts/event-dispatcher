#!/bin/bash

set -e

PARENT_PATH=$(
  cd "$(dirname "${BASH_SOURCE[0]}")"
  pwd -P
)

cmd=$(printf '%q ' "composer $*")

$PARENT_PATH/env dev exec event-dispatcher runuser --preserve-environment hostuser -c "$cmd"
